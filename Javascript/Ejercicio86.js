//Accede a propiedades de objetos con variables

// Configuración
const testObj = {
    12: "Namath",
    16: "Montana",
    19: "Unitas"
  };
  
 // Cambia solo el código debajo de esta línea
const playerNumber = 16;  // Cambia esta línea
const player = testObj[playerNumber];   // Cambia esta línea

  /*

1-  Asignamos la propiedad a una variable const

        variableConstante1 =  nombrePropiedadObjeto; 

2- luego asignamos el nombre del objeto con la primera variable a la segunda variable const

         variableConstante2 =  nombreObjeto [variableConstante1];

3- Por ultimo llamamos ala variable 2, y lo que va a salir en consola es el valor de la propiedad

        console.log(variableConstante2);
  
  */
