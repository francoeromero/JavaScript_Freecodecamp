//Genera fracciones aleatorias con JavaScript

function randomFraction() {

  return Math.random();  
}

console.log(randomFraction()); // te devuelve un numero del 0 al 1 random

//EJEMPLOS 

/*

function randomNumber() {
  return Math.floor(Math.random() * 10) + 1;
}

console.log(randomNumber()); // puede imprimir un número entre 1 y 10



function randomNumber(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}
console.log(randomNumber(5, 15)); // puede imprimir un número entre 5 y 15




*/