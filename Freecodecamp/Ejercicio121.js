
// Utiliza el operador de propagación para evaluar los arreglos en el lugar

const arr1 = ['JAN', 'FEB', 'MAR', 'APR', 'MAY'];
let arr2;

arr2 = [...arr1];  // Cambie "...arr1" que es el operador de propagacion para copiar todo el contenido de arr1

console.log(arr2);